rich text (markup tags merge global + per useI18n/createI18n registry) (same as functions)
auto html lang, dir: new Intl.Locale('ar-EG'); locale.getTextInfo().direction ??
gen types using cli
finish cli
support namespacing?
messagse loader ??
suspense?
